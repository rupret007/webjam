# PEP 517 tools required to build cryptography 50.0.0 on Intel macOS.
# Runtime dependencies are repeated deliberately so build isolation can stay off.
cffi==2.1.0
maturin==1.14.1
pycparser==3.0
setuptools==81.0.0
