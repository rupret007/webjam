# WebJam - Music Collaboration Platform
# Python dependencies

# Primary UI framework (Qt) — new Conductor UI
PySide6>=6.6.0

# Legacy UI (retained until Qt port reaches parity)
customtkinter>=5.2.0

# For building executables
pyinstaller>=5.13.0

# Core modernization dependencies
numpy>=1.26.0
sounddevice>=0.4.7
soundfile>=0.12.1  # Take Deck: multitrack playback file IO
fastapi>=0.111.0
uvicorn>=0.30.0
httpx>=0.27.0
sentry-sdk>=2.0.0

