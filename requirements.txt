# WebJam - Music Collaboration Platform
# Python dependencies

# Primary UI framework (Qt) — new Conductor UI
PySide6>=6.6.0
# Qt WebEngine — required for embedded Webex pane (Phase 3)
# Included with the PySide6 macOS/Windows wheels; Linux may need a separate install
PySide6-WebEngine>=6.6.0

# Legacy UI (retained until Qt port reaches parity)
customtkinter>=5.2.0

# For building executables
pyinstaller>=5.13.0

# Core modernization dependencies
numpy>=1.26.0
sounddevice>=0.4.7
fastapi>=0.111.0
uvicorn>=0.30.0
httpx>=0.27.0
pycasbin>=1.36.0
sentry-sdk>=2.0.0

