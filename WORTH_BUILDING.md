# Worth-Building — Art host network recovery

The host of a Make together or Paint along room loses its selected local
network address, then regains it. The existing room controller restores the
room overview to open/connected, while the conductor has accepted terminal
failure and keeps offering Reset Invite. That action belongs to native
invitations and rejects the LAN host. An artist sees a recovered room beside
an action that cannot help. The existing membership test stops at address loss.

The same recovery path discards a retained listener when Try Again finds no
usable address. If replacing a listener cannot confirm cleanup, an old address
returning must not make the retained invitation copyable. These are concrete
room ownership and next-action failures, not a new collaboration feature.

One slice: preserve the current listener and local optional work during a
temporary route interruption; show one useful Try Again action; accept a
same-address return under the same attempt using current authenticated-reader
facts; replace a listener only after an explicit retry and confirmed cleanup.
Initial startup failures and unresolved cleanup remain failures. An available
local route is not proof that another artist has joined.

Logging records bounded transitions and explicit retry outcomes. Repeated
polling must remain quiet. No invitation, address, artist identity, Notes,
canvas credentials, or raw private exception belongs in those records.

Verification requires actual controller/HUD journeys for loss/return, missing
address retry, changed-address replacement, failed cleanup, retirement, and
optional activity visibility; compact native UX; focused Art/door/session/
invitation tests; full pytest; privacy and leftover review; and exact-tip hosted
SUCCESS including four desktop builds. Baseline and subsequent failures are
retained in PRE_KAREN_QA.md.

Worth-Building and the final local gate PASS: the actual native baseline
reproduced the dead action. The corrected complete suite passed 8,420 tests;
22 new native cases and 2,604 focused cases passed. An isolated real log-file
round trip proves recovery events reach the redacted, rotating handler without
private payloads or polling spam. Independent leftover/privacy/security/UX
review passed on all six frozen source/test hashes. Exact-tip hosted evidence
and the draft handoff are recorded in the PR and coord AFTER.

Canonical checkout: /Users/jeffstory/Documents/WebJam only. Fresh branch
`codex/webjam-finish-product-art-host-recovery` from verified master
`ff198d790243a819aa1166ea6afa72fa2989380a`, after Bob externally squashed #78.
[BEFORE 5557966863](https://github.com/rupret007/Bob-the-Bot/issues/3#issuecomment-5557966863)
claims 03:15:14–07:15:14 CT on 2026-09-06. All 45 existing branch refs and
four stashes were recorded before checkout; #78's branch remains untouched.

One OPEN DRAFT for Karen, then AFTER and agent:none; stop for Bob conductor.
Art Preview, own tools, silent local-file Paint along, Webex beside WebJam and
never on the Art door. No second media stack or automatic external launch.
Parked #37/#49 untouched; stay off #67. No short-code/public rendezvous,
merge/tag/sign/Pages/Release Trust/publish/GitHub Latest release. Unsigned
0.27.2 is Jeff-only; physical/live-provider/installed-package gates NOT RUN.
Black/white/neutral gray/burnt orange. WebJam only.
